/**
 * enum with names for all metadata maps.
 */
export enum Metadata {
    ROUTES = 'ROUTE_METADATA',
    PARAMETERS = 'PARAM_METADATA',
    MIDDLEWARES = 'MIDDLEWARE_METADATA',
    GUARDS = 'GUARD_METADATA'
}